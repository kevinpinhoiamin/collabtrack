<resources>

    <!-- Tema base -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Tema do projeto -->
    <style name="AppTheme" parent="BaseAppTheme">
        <item name="colorControlHighlight">@color/control_highlight</item>
    </style>

    <!-- Tema do Menu -->
    <style name="AppTheme.TopNavigation" parent="BaseAppTheme">
        <item name="android:actionMenuTextColor">@color/white</item>
    </style>

    <!-- Tema do Bottom Navigation -->
    <style name="AppTheme.BottomNavigation" parent="BaseAppTheme">
        <item name="itemTextColor">@color/black</item>
    </style>

    <!-- Temas para a Toolbar, ver include_toolbar.xml -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="HomeTheme" parent="BaseAppTheme">
        <item name="colorAccent">@color/white</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="colorControlActivated">@color/white</item>
        <item name="colorControlHighlight">@color/white</item>
        <item name="android:textColorHint">@color/white</item>
    </style>

</resources>
